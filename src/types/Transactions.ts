export enum TransactionStatus {
	PENDING = "pending",
	FAILED = "failed",
	COMPLETED = "completed",
	CANCELLED = "cancelled",
}

export enum TransactionType {
	DEPOSIT = "deposit",
	PURCHASE = "purchase",
	WITHDRAWAL = "withdrawal",
}
